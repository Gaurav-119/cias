# Root compose overrides (optional). Copy to .env for docker compose.
SECRET_KEY=change-me-in-production
JWT_SECRET=change-me-jwt-secret

# Payments: record (offline/test) or stripe
PAYMENT_MODE=record
STRIPE_SECRET_KEY=
